name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ '**'  ]

jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run build
      run: make
